// para utilizar o printf
#include <stdio.h>

int main(void) {
    // declarei as variaveis
    // inicializei a variavel "c" realizando uma operação de adição
    int a = 10, b = 5, c = a + b;

    printf("O resultado de %d mais %d eh igual a %d\n", a, b, c);

    return 0;
}